pipeline{
   agent {label slave1}
      tools {
      tool name: 'maven3', type: 'maven'
      }
      stages{
        stage (hello){
           steps{
               echo "this is radhika"
           }
        }
     }
}
